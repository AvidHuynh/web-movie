import React from 'react';
import PropTypes from 'prop-types';

Movie.propTypes = {
    
};

function Movie(props) {
    return (
        <div>
            <h1>Movie</h1>
        </div>
    );
}

export default Movie;